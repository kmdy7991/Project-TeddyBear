server:
  port: 0
spring:
  application:
    name: catalog-service
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:tcp://localhost/~/testdb
#    username: sa
  datasource:
    url: jdbc:mysql://3.106.124.241:3306/test?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    username: root
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver
#  h2:
#    console:
#      enabled: true
#      settings:
#        web-allow-others: true
#      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
#    show-sql: true
    generate-ddl: true
    # ddl, dml console print(pretty)
    properties:
      hibernate:
        format_sql: true
# script(data.sql) ?? ?? hibernate? ???? ???? ????? ? ??? ???? script? ?? ?? ??
#    defer-datasource-initialization: true
# DDL -> schema.sql / DML -> data.sql
# ??? ?? ?? db? ?? data? ?? ? ? ?? (embedded - ??, always - ??, never - ?? ??)
#  sql:
#    init:
#      mode: always

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

logging:
  level:
    org.hibernate.SQL: debug
